<img width="572" height="170" alt="image" src="https://github.com/user-attachments/assets/f0796ea2-af3f-461b-a877-f4eef3aeeaf0" /># Lambda Function in Python: Addition of Two Numbers

## 🎯 Aim
To write a Python program that defines a **lambda function** which takes two arguments `a` and `b`, and returns their sum.

## 🧠 Algorithm
1. Get two integer inputs from the user.
2. Use a **lambda function** to define a function `f` that returns `a + b`.
3. Call the function with the user inputs and print the result.

## 🧾 Program
```python
a = int(input("Enter first number: "))
b = int(input("Enter second number: "))
f = lambda a, b: a + b
print("Sum:", f(a, b))
```
## Output

<img width="572" height="170" alt="image" src="https://github.com/user-attachments/assets/ca72a75b-f10f-4a1f-b27a-1a56c525dbe3" />

## Result
The Python program was successfully executed using a lambda function to find the sum of two numbers.
